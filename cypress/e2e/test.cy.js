// Will add e2e tests here for each uer journey on the site

describe('Test', () => {
  it('checks the test test files are linked', () => {
    // Basic check to test config
    cy.visit('http://localhost:3000')
  })
})